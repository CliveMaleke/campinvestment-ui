import { Meta, Title, Subtitle } from '@storybook/addon-docs/blocks';
import { TabsState } from '@storybook/components';
import { ButtonWithIcon as Button } from './index';
import DeleteIcon from '@components/data-display/Icon/DeleteIcon';

<Title>Button</Title>
<Subtitle>
  Buttons allow users to take actions, and make choices, with a single tap.
</Subtitle>

### Import

```jsx
import Button from 'tokenomy-ui/inputs/Button';
import DeleteIcon from 'tokenomy-ui/data-display/Icon/DeleteIcon';
```

### Usage

<TabsState initial="primary">
  <div id="primary" title="Primary"><div>
  <h3 style={{ marginTop: '20px' }}>Start Icon</h3>
<Button variant="contained" className="MuiButton-sizeExtraLarge2x" startIcon={<DeleteIcon />} style={{ marginTop: 20 }}>Delete</Button>

```jsx
<Button variant="contained" className="MuiButton-sizeExtraLarge2x" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" className="MuiButton-sizeExtraLarge" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" className="MuiButton-sizeExtraLarge" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" size="large" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" size="large" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" size="small" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" size="small" startIcon={<DeleteIcon />}>Delete</Button>
```
  <h3>End Icon</h3>
<Button variant="contained" className="MuiButton-sizeExtraLarge2x" endIcon={<DeleteIcon />} style={{ marginTop: 20 }}>Delete</Button>

```jsx
<Button variant="contained" className="MuiButton-sizeExtraLarge2x" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" className="MuiButton-sizeExtraLarge" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" className="MuiButton-sizeExtraLarge" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" size="large" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" size="large" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" size="small" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" size="small" endIcon={<DeleteIcon />}>Delete</Button>
```
  </div></div>
  <div id="secondary" title="Secondary"><div>
  <h3 style={{ marginTop: '20px' }}>Start Icon</h3>
<Button variant="contained" color="secondary" className="MuiButton-sizeExtraLarge2x" startIcon={<DeleteIcon />} style={{ marginTop: 20 }}>Delete</Button>

```jsx
<Button variant="contained" color="secondary" className="MuiButton-sizeExtraLarge2x" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" color="secondary" className="MuiButton-sizeExtraLarge" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" color="secondary" className="MuiButton-sizeExtraLarge" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" size="large" color="secondary" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" size="large" color="secondary" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" color="secondary" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" color="secondary" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" size="small" color="secondary" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" size="small" color="secondary" startIcon={<DeleteIcon />}>Delete</Button>
```
  <h3>End Icon</h3>
<Button variant="contained" color="secondary" className="MuiButton-sizeExtraLarge2x" endIcon={<DeleteIcon />} style={{ marginTop: 20 }}>Delete</Button>

```jsx
<Button variant="contained" color="secondary" className="MuiButton-sizeExtraLarge2x" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" color="secondary" className="MuiButton-sizeExtraLarge" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" color="secondary" className="MuiButton-sizeExtraLarge" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" size="large" color="secondary" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" size="large" color="secondary" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" color="secondary" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" color="secondary" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" size="small" color="secondary" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" size="small" color="secondary" endIcon={<DeleteIcon />}>Delete</Button>
```
  </div></div>
  <div id="disabled" title="Disabled"><div>
  <h3 style={{ marginTop: '20px' }}>Start Icon</h3>
<Button variant="contained" disabled className="MuiButton-sizeExtraLarge2x" startIcon={<DeleteIcon />} style={{ marginTop: 20 }}>Delete</Button>

```jsx
<Button variant="contained" disabled className="MuiButton-sizeExtraLarge2x" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" disabled className="MuiButton-sizeExtraLarge" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" disabled className="MuiButton-sizeExtraLarge" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" disabled size="large" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" disabled size="large" startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" disabled startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" disabled startIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" disabled size="small" startIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" disabled size="small" startIcon={<DeleteIcon />}>Delete</Button>
```
  <h3>End Icon</h3>
<Button variant="contained" disabled className="MuiButton-sizeExtraLarge2x" endIcon={<DeleteIcon />} style={{ marginTop: 20 }}>Delete</Button>

```jsx
<Button variant="contained" disabled className="MuiButton-sizeExtraLarge2x" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" disabled className="MuiButton-sizeExtraLarge" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" disabled className="MuiButton-sizeExtraLarge" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" disabled size="large" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" disabled size="large" endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" disabled endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" disabled endIcon={<DeleteIcon />}>Delete</Button>
```
<Button variant="contained" disabled size="small" endIcon={<DeleteIcon />}>Delete</Button>

```jsx
<Button variant="contained" disabled size="small" endIcon={<DeleteIcon />}>Delete</Button>
```
  </div></div>

</TabsState>
