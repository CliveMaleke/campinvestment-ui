import { Meta, Title, Subtitle } from '@storybook/addon-docs/blocks';
import { TabsState } from '@storybook/components';
import { Button as IconButton } from './index';
import DeleteIcon from '@components/data-display/Icon/DeleteIcon';

<Meta title="Components/Input/Button" />

<Title>Icon Button</Title>
<Subtitle>
  Icons are also appropriate for toggle buttons that allow a single choice to be selected or deselected, such as adding or removing a star to an item.
</Subtitle>

### Import

```jsx
import IconButton from 'tokenomy-ui/inputs/IconButton';
import DeleteIcon from 'tokenomy-ui/data-display/Icon';
```

### Usage

<TabsState initial="primary">
  <div id="primary" title="Primary"><div>
<IconButton style={{ marginTop: 20 }}>
  <DeleteIcon />
</IconButton>

```jsx
<IconButton>
  <DeleteIcon />
</IconButton>
```

  </div></div>
  <div id="secondary" title="Secondary"><div>
<IconButton color="secondary" style={{ marginTop: 20 }}>
  <DeleteIcon />
</IconButton>

```jsx
<IconButton color="secondary">
  <DeleteIcon />
</IconButton>
```

  </div></div>
  <div id="default" title="Default"><div>
<IconButton color="default" style={{ marginTop: 20 }}>
  <DeleteIcon />
</IconButton>

```jsx
<IconButton color="default">
  <DeleteIcon />
</IconButton>
```

  </div></div>
  <div id="disabled" title="Disabled"><div>
<IconButton disabled style={{ marginTop: 20 }}>
  <DeleteIcon />
</IconButton>

```jsx
<IconButton disabled>
  <DeleteIcon />
</IconButton>
```

  </div></div>

</TabsState>
