import { Title, Subtitle } from '@storybook/addon-docs/blocks';
import TypographyBody from './index';

<Title>Button</Title>
<Subtitle>Button text is used to define the font that matches the button character.</Subtitle>

### Import

```jsx
import Typography from 'tokenomy-ui/data-display/Typography';
```

### Usage

<TypographyBody type="semibold" buttonScale="small" style={{ marginTop: 20 }} />

```jsx
<Typography variant="button" type="semibold" buttonScale="small">Button</Typography>
```

<TypographyBody type="semibold" buttonScale="medium" />

```jsx
<Typography variant="button" type="semibold" buttonScale="medium">Button</Typography>
```

<TypographyBody type="semibold" buttonScale="large" />

```jsx
<Typography variant="button" type="semibold" buttonScale="large">Button</Typography>
```

<TypographyBody type="semibold" buttonScale="extralarge" />

```jsx
<Typography variant="button" type="semibold" buttonScale="extralarge">Button</Typography>
```

<TypographyBody type="semibold" buttonScale="extralarge2x" />

```jsx
<Typography variant="button" type="semibold" buttonScale="extralarge2x">Button</Typography>
```
