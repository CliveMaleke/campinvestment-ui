import { Title, Subtitle } from '@storybook/addon-docs/blocks';
import {
  AlertOutlinedGeneral,
  AlertOutlinedError,
  AlertOutlinedWarning,
  AlertOutlinedInfo,
  AlertOutlinedSuccess,
} from './index';

<Title>Alert Variants</Title>
<Subtitle>
  Two additional variants are available – outlined, and filled:
</Subtitle>

### Import

```jsx
import Alert from 'tokenomy-ui/feedback/Alert';
import CheckFilledIcon from 'tokenomy-ui/data-display/Icon/CheckFilledIcon';
import AlertIcon from 'tokenomy-ui/data-display/Icon/AlertIcon';
import CloseFilledIcon from 'tokenomy-ui/data-display/Icon/CloseFilledIcon';
import InfoIcon from 'tokenomy-ui/data-display/Icon/InfoIcon';
```

### Usage

<AlertOutlinedGeneral />

```jsx
<Alert className="MuiAlert-standardOutlinedGeneral">
  This is a general alert — check it out!
</Alert>
```

<AlertOutlinedError />

```jsx
<Alert variant="outlined" severity="error" icon={<CloseFilledIcon />}>
  This is an error alert — check it out!
</Alert>
```

<AlertOutlinedWarning />

```jsx
<Alert variant="outlined" severity="warning" icon={<AlertIcon />}>
  This is a warning alert — check it out!
</Alert>
```

<AlertOutlinedInfo />

```jsx
<Alert variant="outlined" severity="info" icon={<InfoIcon />}>
  This is an info alert — check it out!
</Alert>
```

<AlertOutlinedSuccess />

```jsx
<Alert variant="outlined" severity="success" icon={<CheckFilledIcon />}>
  This is a success alert — check it out!
</Alert>
```
