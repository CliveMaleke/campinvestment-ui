import { Title, Subtitle } from '@storybook/addon-docs/blocks';
import {
  AlertOutlinedGeneral,
  AlertOutlinedError,
  AlertOutlinedWarning,
  AlertOutlinedInfo,
  AlertOutlinedSuccess,
} from './index';

<Title>Alert Variants</Title>
<Subtitle>
  Two additional variants are available – outlined, and filled:
</Subtitle>

### Import

```jsx
import React from 'react';
import Alert from 'tokenomy-ui/feedback/Alert';
import CheckFilledIcon from '@components/data-display/Icon/CheckFilledIcon';
import AlertIcon from '@components/data-display/Icon/AlertIcon';
import CloseFilledIcon from '@components/data-display/Icon/CloseFilledIcon';
import InfoIcon from '@components/data-display/Icon/InfoIcon';
```

### Usage

<AlertOutlinedGeneral />

```jsx
export const AlertOutlinedGeneral = (props) => (
  <Alert className="MuiAlert-standardOutlinedGeneral" {...props}>
    This is a general alert — check it out!
  </Alert>
);
```

<AlertOutlinedError />

```jsx
export const AlertOutlinedError = (props) => (
  <Alert
    variant="outlined"
    severity="error"
    icon={<CloseFilledIcon />}
    {...props}
  >
    This is an error alert — check it out!
  </Alert>
);
```

<AlertOutlinedWarning />

```jsx
export const AlertOutlinedWarning = (props) => (
  <Alert variant="outlined" severity="warning" icon={<AlertIcon />} {...props}>
    This is a warning alert — check it out!
  </Alert>
);
```

<AlertOutlinedInfo />

```jsx
export const AlertOutlinedInfo = (props) => (
  <Alert variant="outlined" severity="info" icon={<InfoIcon />} {...props}>
    This is an info alert — check it out!
  </Alert>
);
```

<AlertOutlinedSuccess />

```jsx
export const AlertOutlinedSuccess = (props) => (
  <Alert
    variant="outlined"
    severity="success"
    icon={<CheckFilledIcon />}
    {...props}
  >
    This is a success alert — check it out!
  </Alert>
);
```
