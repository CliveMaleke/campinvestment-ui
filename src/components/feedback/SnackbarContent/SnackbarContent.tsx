// Vendors
import React from 'react';
import { SnackbarContent, SnackbarContentProps } from '@material-ui/core';

export default (props: SnackbarContentProps) => {
  return <SnackbarContent {...props} />;
};
