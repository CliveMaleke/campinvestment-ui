// Vendors
import React from 'react';
import FormControlLabel, {
  FormControlLabelProps,
} from '@material-ui/core/FormControlLabel';

export default (props: FormControlLabelProps) => {
  return <FormControlLabel {...props} />;
};
